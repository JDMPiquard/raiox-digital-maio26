/**
 * Generated by orval v8.9.1 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import * as zod from 'zod';


/**
 * Returns server health status
 * @summary Health check
 */
export const HealthCheckResponse = zod.object({
  "status": zod.string()
})


/**
 * Returns the most recently cached payload for `sid`, or 404 if nothing
has been cached for it. Used to render shared links (`/r/:sid`) and
WhatsApp-delivered results without re-running the diagnostic.

 * @summary Read a cached assessment result
 */
export const GetCachedResultParams = zod.object({
  "sid": zod.coerce.string()
})

export const GetCachedResultResponse = zod.object({
  "sid": zod.string(),
  "shopName": zod.string().nullish(),
  "payload": zod.record(zod.string(), zod.unknown()),
  "cachedAt": zod.coerce.date(),
  "hasEmail": zod.boolean()
})


/**
 * Upserts the full diagnostic payload for `sid` so it can be re-served
instantly when the result link is shared.

 * @summary Cache an assessment result by sid
 */
export const CacheResultParams = zod.object({
  "sid": zod.coerce.string()
})

export const CacheResultBody = zod.object({
  "shopName": zod.string().optional(),
  "payload": zod.record(zod.string(), zod.unknown()).describe('Full diagnostic result payload.')
})

export const CacheResultResponse = zod.object({
  "ok": zod.boolean()
})


/**
 * Stores (upserts) the email to receive a Raio-X result link by `sid`.
If the assessment is already cached, the email is sent now via Resend
regardless of the `immediate` flag; otherwise it is queued and sent
when the diagnostic completes (via the cache or dispatch endpoint).
The `immediate` field is accepted for backwards compatibility but no
longer gates the send.

 * @summary Capture an email for a result
 */
export const CaptureResultEmailParams = zod.object({
  "sid": zod.coerce.string()
})

export const captureResultEmailBodyImmediateDefault = false;

export const CaptureResultEmailBody = zod.object({
  "email": zod.string().email(),
  "immediate": zod.boolean().default(captureResultEmailBodyImmediateDefault).describe('If true, send the email now instead of queuing.'),
  "shopName": zod.string().optional().describe('Optional shop name to personalise the email.')
})

export const CaptureResultEmailResponse = zod.object({
  "ok": zod.boolean(),
  "sent": zod.boolean().optional(),
  "queued": zod.boolean().optional()
})


/**
 * Called when the diagnostic for `sid` has just completed. If an email
is on file and hasn't been sent yet, sends the Raio-X result link now.

 * @summary Flush any queued result email for the given sid
 */
export const DispatchResultEmailParams = zod.object({
  "sid": zod.coerce.string()
})

export const DispatchResultEmailBody = zod.object({
  "shopName": zod.string().optional()
})

export const DispatchResultEmailResponse = zod.object({
  "ok": zod.boolean(),
  "sent": zod.boolean().optional(),
  "queued": zod.boolean().optional()
})


